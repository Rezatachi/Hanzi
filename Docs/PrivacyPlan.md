# Privacy Plan

- Local store is the source of truth.
- App Group data contains only widget-safe display fields.
- Analytics is local and debug-only unless replaced.
- No email, notes, or sensitive personal data is logged by default.
- No hardcoded secrets.
- Backend adapters are protocol-based and optional.
- Delete/export hooks belong in Profile settings before launch.
